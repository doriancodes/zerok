[package]
name = "zerok"
version = "0.1.0"
edition = "2024"

[dependencies]
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
tempfile = "3"
